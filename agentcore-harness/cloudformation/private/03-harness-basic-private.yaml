# Stack 3/3 (private) — Harness in VPC mode. No public endpoint; all traffic stays in VPC.
# Deploy after 01-iam and 02-network. Pass their outputs as parameters.
AWSTemplateFormatVersion: "2010-09-09"
Description: AgentCore Harness (VPC mode) in private subnets

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: Naming
        Parameters:
          - ApplicationName
          - Environment
      - Label:
          default: IAM (from stack 01-iam)
        Parameters:
          - HarnessExecutionRoleArn
      - Label:
          default: Network (existing VPC + from stack 02-network)
        Parameters:
          - PrivateSubnetIds
          - HarnessSecurityGroupId
      - Label:
          default: Agent configuration
        Parameters:
          - ModelId
          - MaxIterations
          - TimeoutSeconds
    ParameterLabels:
      ApplicationName:
        default: Application name prefix
      Environment:
        default: Deployment environment (dev / stg / prd)
      HarnessExecutionRoleArn:
        default: Harness execution role ARN (01-iam output)
      PrivateSubnetIds:
        default: Private subnet IDs (comma-separated, same list passed to 02-network)
      HarnessSecurityGroupId:
        default: Harness security group ID (02-network output)
      ModelId:
        default: Bedrock model ID
      MaxIterations:
        default: Max agent loop iterations per invocation
      TimeoutSeconds:
        default: Max duration per invocation (seconds)

Parameters:
  ApplicationName:
    Type: String
    Default: coreagent
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, stg, prd]
  HarnessExecutionRoleArn:
    Type: String
    Description: HarnessExecutionRoleArn from stack 01-iam
  PrivateSubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: Comma-separated list of existing private subnet IDs
  HarnessSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
    Description: HarnessSecurityGroupId from stack 02-network
  ModelId:
    Type: String
    Default: global.anthropic.claude-sonnet-4-6
  MaxIterations:
    Type: Number
    Default: 100
  TimeoutSeconds:
    Type: Number
    Default: 300

Resources:

  # --- Harness in VPC mode — ENIs placed in private subnets, no public endpoint ---
  Harness:
    Type: AWS::BedrockAgentCore::Harness
    Properties:
      HarnessName: !Sub "${ApplicationName}_${Environment}_vpc_basic"  # HarnessName forces replacement on change
      ExecutionRoleArn: !Ref HarnessExecutionRoleArn
      Model:
        BedrockModelConfig:
          ModelId: !Ref ModelId
          ApiFormat: converse_stream
      SystemPrompt:
        - Text: !Sub "You are a helpful assistant deployed by ${ApplicationName} in the ${Environment} environment."
      Environment:
        AgentCoreRuntimeEnvironment:
          NetworkConfiguration:
            NetworkMode: VPC  # places harness ENIs in private subnets; no inbound public endpoint
            NetworkModeConfig:
              Subnets: !Ref PrivateSubnetIds
              SecurityGroups:
                - !Ref HarnessSecurityGroupId
      MaxIterations: !Ref MaxIterations
      TimeoutSeconds: !Ref TimeoutSeconds
      Truncation:
        Strategy: sliding_window
      Tags:
        - Key: Application
          Value: !Ref ApplicationName
        - Key: Environment
          Value: !Ref Environment

Outputs:
  HarnessArn:
    Value: !GetAtt Harness.Arn
  HarnessId:
    Value: !GetAtt Harness.HarnessId
  RuntimeArn:
    Value: !GetAtt Harness.Environment.AgentCoreRuntimeEnvironment.AgentRuntimeArn
